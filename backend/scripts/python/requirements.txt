# Core OCR dependencies (Google Document AI)
google-cloud-documentai>=2.30.0
google-auth>=2.35.0
Pillow>=10.4.0
pdfplumber>=0.11.4
PyMuPDF>=1.24.13
PyPDF2>=3.0.1
numpy>=1.21.0

# Kiri OCR (Khmer-specialized local OCR engine)
# Transformer-based model optimized for Khmer + English text
kiri-ocr>=0.2.15

# PaddleOCR (local OCR engine for structured English + numeric lab data)
# Requires Python 3.10-3.12 (Bookworm ships 3.11)
# For GPU builds, paddlepaddle is swapped with paddlepaddle-gpu in the Dockerfile
paddlepaddle==3.3.1
paddleocr==3.5.0

# HTTP client for local Ollama LLM API calls
requests>=2.31.0
