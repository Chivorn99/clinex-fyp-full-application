# Core OCR dependencies (Google Document AI)
google-cloud-documentai>=2.30.0
google-auth>=2.35.0
Pillow>=10.4.0
pdfplumber>=0.11.4
PyMuPDF>=1.24.13
PyPDF2>=3.0.1
numpy>=1.21.0

# PaddleOCR is optional — only install if you need the fallback OCR engine.
# It requires Python 3.10-3.12 and paddlepaddle, which may not be
# available on all platforms.
# To install: pip install paddlepaddle>=3.0.0 paddleocr>=2.10.0
